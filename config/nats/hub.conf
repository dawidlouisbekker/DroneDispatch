# Cloud-region NATS hub. Edge zones connect to it as leaf nodes through toxiproxy.
# TODO(milestone 1): one NATS account per service, limited to its own subjects.
server_name: hub
port: 4222
http_port: 8222

jetstream {
  domain: hub
  store_dir: /data/jetstream
}

leafnodes {
  port: 7422
}
